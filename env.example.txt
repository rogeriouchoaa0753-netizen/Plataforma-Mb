# Configuração do Servidor
# Copie este arquivo para .env e configure os valores

PORT=3001
NODE_ENV=development

# Segurança - JWT Secret
# IMPORTANTE: Gere uma chave secreta segura em produção!
# Você pode gerar uma usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=seu_secret_key_aqui_mude_em_producao

# Banco de Dados
# O SQLite criará o arquivo database.db automaticamente
DB_PATH=./database.db

